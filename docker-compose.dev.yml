version: "3.8"
services:
  flask-app:
    ports:
      - 5000:5000
    build: back-end
    environment: 
      ENV: development
      FLASK_APP: /code/server.py
      FLASK_ENV: development
    volumes:
      - ./back-end:/code